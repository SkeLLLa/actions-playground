# on:
#   create:
#     tags:
#       - v[0-9]+.[0-9]+.[0-9]+
#       - v[0-9]+.[0-9]+.[0-9]+-rc.[0-9]+
on:
  workflow_run:
    workflows:
      - Verify
    types:
      - completed
    branches:
      - next

name: Create Release next

jobs:
  release:
    # name: Create release
    runs-on: ubuntu-latest
    steps:
      - run: echo ${{ github.ref }}
